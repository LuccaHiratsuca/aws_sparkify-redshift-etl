# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# --- Credentials ------------------------------------------------------------
# Never commit a dwh.cfg that has been filled in with real keys/passwords.
# Keep local secrets in dwh.local.cfg (ignored) or export them as env vars.
dwh.local.cfg
*.pem
.aws/
credentials.csv

# --- Tooling / OS -----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
