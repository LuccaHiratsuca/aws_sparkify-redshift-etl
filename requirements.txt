# Provision and tear down the Redshift cluster and IAM role (iac.py).
boto3>=1.34.0

# PostgreSQL wire protocol client -- Redshift speaks it, so this is the driver
# used by create_tables.py, etl.py, quality_checks.py and analytics.py.
psycopg2-binary>=2.9.9

# Table rendering for the analytics report (analytics.py).
tabulate>=0.9.0
